import { StyleSheet } from "react-native";

export default StyleSheet.create({
    profileImage: {
        width: 200,
        height: 200,
        borderRadius: 200,
    },
    name: {
        fontSize: 30,
    }
});
