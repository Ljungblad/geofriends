import { StyleSheet } from "react-native";
//import colors from "../../styles/colors";

export default StyleSheet.create({
  container: {
    alignItems: "center",
    height: 300,
    width: 300,
    borderRadius: 5,
  },
  buttonContainer: {
    width: "100%",
    alignItems: "flex-end",
  },
});
