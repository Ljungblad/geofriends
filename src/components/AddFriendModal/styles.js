import { StyleSheet } from "react-native";

export default StyleSheet.create({
  title: {
    marginBottom: 10,
  },
  test: {
    //height: 20,
    // backgroundColor: "grey",
    marginBottom: 20,
  },
  errorMsg: {
    color: "red",
    fontSize: 14,
  }
});
