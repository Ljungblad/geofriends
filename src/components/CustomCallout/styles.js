import { StyleSheet } from "react-native";

export default StyleSheet.create({
    text: {
        fontSize: 18,
    },
    descriptionContainer: {
        marginTop: 2,
        marginBottom: 8,
  },
});
