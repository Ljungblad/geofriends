import { StyleSheet } from "react-native";

export default StyleSheet.create({
  wrapper: {
    alignSelf: "flex-end",
    alignItems: "center",
    justifyContent: "center",
    margin: 20,
  },
});
